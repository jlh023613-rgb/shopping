<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shopping.mapper.ProductImageMapper">
    <insert id="insert" parameterType="com.example.shopping.entity.ProductImage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_images (product_id, image_url, sort_order)
        VALUES (#{productId}, #{imageUrl}, #{sortOrder})
    </insert>
    <select id="findByProductId" resultType="com.example.shopping.entity.ProductImage">
        SELECT id, product_id as productId, image_url as imageUrl, sort_order as sortOrder
        FROM product_images WHERE product_id = #{productId} ORDER BY sort_order, id
    </select>
</mapper>
